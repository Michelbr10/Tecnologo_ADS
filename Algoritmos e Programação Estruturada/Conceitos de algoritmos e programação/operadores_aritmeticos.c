#include <stdio.h>

int main()
{
    float a = 10, b = 5;

    float soma = a + b;
    float subtracao = a - b;
    float multiplicacao = a * b;
    float divisao = a / b;

    printf(" Soma: %f \n Subtracao: %f \n Multiplicacao: %f \n Divisao: %f \n", soma, subtracao, multiplicacao, divisao);

    return 0;
}